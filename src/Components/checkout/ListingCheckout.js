import React from "react";

const ListingCheckout = ({ cart }) => {
  return (
    <div>
      {/* {cart &&
        cart.map((listing) => {
          return (
            <div key={listing.id}>
              <div className="name centered">

              {listing.propertyID}
              </div>
              {listing.propertyID}
            </div>
          );
        })} */}
    </div>
  );
};

export default ListingCheckout;
